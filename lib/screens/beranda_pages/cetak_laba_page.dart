import 'package:flutter/material.dart';

class CetakLabaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Cetak Laba")),
      body: const Center(child: Text("Halaman Cetak Laba")),
    );
  }
}
