import 'package:flutter/material.dart';

class CatatanHutangPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Catatan Hutang")),
      body: const Center(child: Text("Halaman Catatan Hutang")),
    );
  }
}
