import 'package:flutter/material.dart';

class AnalisisDataPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Analisis Dat")),
      body: const Center(child: Text("Halaman Analisis Data")),
    );
  }
}
